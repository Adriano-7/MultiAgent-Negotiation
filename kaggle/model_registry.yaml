# Maps HuggingFace model IDs → Kaggle model source paths.
# Populated by: python kaggle/upload_models.py --size <size>
# Keys must exactly match the model IDs in configs/experiments.yaml.
#
# kaggle_source format: "<owner>/<slug>/transformers/default/<version>"
# Kaggle mounts the files at:  /kaggle/input/<slug>/transformers/default/<version>/
models: {}
